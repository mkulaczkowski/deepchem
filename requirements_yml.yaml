name: clusterone
channels:
  - pandas
  - deepchem
  - rdkit
  - conda-forge
  - omnia
  - defaults
dependencies:
  - python=3.5
  - deepchem=2.1.0

from deepchem.examples.membrane_permeability import membrane_permeability_datasets
